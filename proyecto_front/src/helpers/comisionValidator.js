export function comisionValidator(comision) {
   const pattern = new RegExp('^[0-9]+$', 'i');
  //  if (!comision) return "Ingresar un numero del 1 al 100."
  //  if (!comision) return "El campo es obligatorio."
  //  if(!pattern.test(comision)) return 'Solo debes ingresar numeros enteros'
  //  if(comision < 1 || comision > 100) return 'Ingresar un numero del 1 al 100'
   
    return ''
  }

  